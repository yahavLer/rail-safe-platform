apiVersion: v1
kind: Service
metadata:
  name: hello-yaml
  namespace: safebox-dev

spec:
  selector:
    app: hello-yaml

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

  type: ClusterIP
