apiVersion: v1
kind: ConfigMap
metadata:
  name: base44-bridge-config
  namespace: safebox-dev
data:
  PORT: "3001"
  NODE_ENV: "production"
  BASE44_SERVER_URL: ${BASE44_SERVER_URL}
  BASE44_FUNCTIONS_VERSION: ${BASE44_FUNCTIONS_VERSION}
