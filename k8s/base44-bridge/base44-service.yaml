apiVersion: v1
kind: Service
metadata:
  name: base44-bridge
  namespace: safebox-dev
spec:
  selector:
    app: base44-bridge
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
